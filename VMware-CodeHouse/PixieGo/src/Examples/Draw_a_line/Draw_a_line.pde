// Using processing 3.1.2 
// Mode is set - Java
// Using Size, background, line syntaxes to draw line on specified window size
//set the output screen's size
size(200, 200);

//set the color of the screen
background(0);
//set the color for the line to draw
stroke(255,255,255);
//draw the line : line(x1,y1,x2,y2)
line(50, 0, 50, height);  //height is the height of the screen